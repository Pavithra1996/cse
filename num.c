#include<stdio.h>
#include<conio.h>
int main()
{
int n;
printf("Enter the no of elements to be added");
scanf("%d",&n);
for(i=0;i<=n;i++)
printf("enter the no to be added");
scanf("%d",&a);
sum=sum+a;
return 0;
}
